import { ACTIONS } from '../constants';
const defaultState = {
	posts: [],
	erorr: null,
};
export function postListReducer(state, action) {
	switch (action.type) {
		case value:
			break;

		default:
			return defaultState;
	}
}
